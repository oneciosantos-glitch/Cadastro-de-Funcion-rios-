# Copie este arquivo como secrets.toml e defina uma senha:
#   cp secrets.toml.exemplo secrets.toml
# Depois edite o valor da senha.
# Se SISTEMA_SENHA estiver definida no ambiente, ela tem prioridade.
#
# Sem nenhuma senha configurada o sistema fica aberto para quem tiver o link.

senha = "suaSenhaAqui"
